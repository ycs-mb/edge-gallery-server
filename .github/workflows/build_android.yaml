name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ "main", "claude/**" ]
    paths:
      - 'Android/**'
      - '.github/workflows/build_android.yaml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'Android/**'

jobs:
  build_apk:
    name: Build Android APK
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Android/src
    steps:
      - name: Checkout the source code
        uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      # ---- Full flavor (existing Edge Gallery app) ----
      - name: Build full debug APK
        run: ./gradlew assembleFullDebug
      - name: Build full release APK
        run: ./gradlew assembleFullRelease
      - name: Upload full debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-full-debug-apk
          path: Android/src/app/build/outputs/apk/full/debug/*.apk
          if-no-files-found: error
      - name: Upload full release APK
        uses: actions/upload-artifact@v4
        with:
          name: app-full-release-apk
          path: Android/src/app/build/outputs/apk/full/release/*.apk
          if-no-files-found: warn

      # ---- Serveronly flavor (standalone headless server APK) ----
      - name: Build serveronly debug APK
        run: ./gradlew assembleServeronlyDebug
      - name: Upload serveronly debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-serveronly-debug-apk
          path: Android/src/app/build/outputs/apk/serveronly/debug/*.apk
          if-no-files-found: error
